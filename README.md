# Dotfiles

## Installation

Requires Arch Linux.

```
sudo pacman -S archlinux-keyring
sudo pacman -Syu
sudo pacman -S git
cd ~
git init
git remote add origin git@github.com:sigurdo/dotfiles.git
git fetch
git checkout -f main
```
